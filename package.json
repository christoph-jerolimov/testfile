{
  "name": "testfile-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Testfile — a declarative YAML format for running test suites with services, matrices and dynamic ports",
  "license": "Apache-2.0",
  "workspaces": [
    "schema",
    "core-ts",
    "sync-ts",
    "mcp-ts",
    "tui-ts",
    "web-ts",
    "runner-ts",
    "viewer-ts",
    "viewer-web",
    "vscode-extension",
    "website",
    "conformance"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "oxfmt": "^0.63.0",
    "oxlint": "^1.77.0"
  }
}
