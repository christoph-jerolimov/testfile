version: 0
forwardEnv: [CONFORMANCE_FLAG]
test:
  name: root
  sequence:
    - name: on
      if: ${{ env.CONFORMANCE_FLAG }} == yes
      command: "true"
    - name: off
      if: ${{ env.CONFORMANCE_UNSET_FLAG }}
      command: "false"
